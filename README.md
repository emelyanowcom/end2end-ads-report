# typhoon.coffee RSS feed

Static `feed.xml` served via GitHub Pages, referenced by a redirect from
`https://typhoon.coffee/feed.xml` (configured in Framer).

Regenerated by `.github/workflows/rss-feed.yml` on `main` (see that branch),
which pulls fresh articles from the Framer CMS and pushes the updated
`feed.xml` here.
